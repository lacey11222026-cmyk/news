@using BIZ
@using UTILS
@using WebMVC4.Helper
@model BIZ.Entity.CATEGORY_FULL
@using WebMVC4.Helper
@{
    var langpage = WorkContext.GetLanguage();
}

@if (Model != null)
{
    <h2 class="title-cm01 title-inline mt20"><a href="@Model.Url">@Model.Name</a></h2>

}
