@model List<DATA.Banner>

@if (Model != null)
{
    foreach (var item in Model.Select((value, i) => new { i, value }))
    {
        <a href="@item.value.Url"><img src="@item.value.Data" alt="" class="w-100"></a>
    }


}


