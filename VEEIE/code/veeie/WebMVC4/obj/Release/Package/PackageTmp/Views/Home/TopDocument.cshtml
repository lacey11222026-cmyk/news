@using BIZ
@using UTILS
@using WebMVC4.Helper
@model List<BIZ.Entity.DOCUMENT_FULL>
@if (Model != null)
{
    if (Model.Count > 0)
    {

        <h2 class="title-cm"><a href="/Document/Index/@(ViewBag.cateId)">@ViewBag.cateName</a></h2>
        <ul class="document-list">
            @foreach (var item in Model.Select((value, i) => new { i, value }))
            {
                <li>
                    <a href="@Utils.FormatUrlRewrite(item.value.Id, item.value.Name, "DocumentDetail")"
                       title="@item.value.Name">
                        @Html.Raw(item.value.Name)

                    </a>
                </li>
            }
        </ul>


    }

}
