@model WebMVC4.Models.PaginationInfo
@using WebMVC4.Helper
@if (Model.Pages.Count() > 1)
{
    if (Model.CurrentPage > 10)
    {
        @*<li><a href="@Url.SetParameter("Page", 1)" ><<</a> </li>*@
    }
    if (Model.HasPrevPage)
    {

        <li><a class="next page-numbers" href="@Model.PrevPageUrl"><</a> </li>
    }
    @* else
        {
        <li><a href="#" class="active">Trước</a> </li>      }*@
    foreach (var p in Model.Pages)
    {
        if (p == Model.CurrentPage)
        {
            <li class="active"><span aria-current="page" class="page-numbers current">@p</span></li>

        }
        else
        {

            <li><a class="page-numbers" href="@Model.PageUrlFor(p)">@p</a> </li>
        }
    }
    if (Model.HasNextPage)
    {

        <li class="next page-numbers"><a href="@Model.NextPageUrl"> > </a></li>
    }
    @* else
        {
        <li><a href="#" class="active">Sau </a></li>
        }*@

    <div class="clear"></div>
}