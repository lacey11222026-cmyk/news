@model List<DATA.Banner>
@using WebMVC4.Helper
@{
    var langpage = WorkContext.GetLanguage();
}
@if (Model != null)
{
    foreach (var item in Model.Select((value, i) => new { i, value }))
    {
        @*if (langpage == "vi-vn")
            {
                <li><a href="@item.value.Url"><img src="@item.value.Data" alt="" class="w-100"></a></li>
            }
            else
            {
                <li><a href="@item.value.Url"><img src="@item.value.Description" alt="" class="w-100"></a></li>
            }*@
        <div class="swiper-slide" style="background-image: url('@item.value.Data');">
            <div class="item">
                <h2>@item.value.Name</h2>
                <p>@Html.Raw(item.value.Description)</p>
            </div>
        </div>
       

    }

}


