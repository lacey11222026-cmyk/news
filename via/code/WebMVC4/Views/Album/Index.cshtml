@using BIZ
@using UTILS
@using WebMVC4.Helper
@model WebMVC4.Models.AlbumModel
@{
    var langpage = WorkContext.GetLanguage();
}

<div class="image-page container mt20">
    <div class="row">
        <div class="col-lg-8 col-md-12 pb40">
            @{Html.RenderAction("Breadcrumb", "News", new { CategoryId = Model.CategoryId });}
            <ul class="row video-list mt20">

                @foreach (var item in Model.listdata.Select((value, i) => new { i, value }))
                {

                   
                    <li class="col-md-4 col-6">
                        <a href="@Utils.FormatUrlRewrite(item.value.Id, item.value.Title, "AlbumDetail")"><img src="@item.value.MainImage?w=491&h=291&mode=crop" alt="" class="w-100"></a>
                        <a href="@Utils.FormatUrlRewrite(item.value.Id, item.value.Title, "AlbumDetail")" class="mt15 font-weight-bold dis-block">@item.value.Title</a>
                        <p class="newsdate mt15">@item.value.PublishDate.Value.ToString("dd/MM/yyyy")</p>
                    </li>
                }
            </ul>
            <div class="paging mt50 text-center">
                <ul>
                    @{Html.RenderPartial("Pagination", new WebMVC4.Models.PaginationInfo
                        {
                            PageUrl = Url.SetParameter("Page", "!0"),
                            CurrentPage = Model.pageIndex,
                            PageSize = Model.pageSize,
                            TotalItemCount = Model.total,
                        }); }
                </ul>
            </div>
        </div><!-- end content left-->

        <div class="col-lg-4 col-md-12">
            <div class="noti-box">
                @{Html.RenderAction("RightLastestNews2", "News", new { CateName = "", CategoryId = 93, MaxLastestNews = 3, cssClass = "maxheight02" });}

            </div>
            <ul class="link-list mt20">
                @{Html.RenderAction("BannerRight", "Home", new { lang = langpage });}

            </ul>
        </div>
    </div>
</div>
<!-- end content-->




<script type="text/javascript">


    $('#menu_66').addClass("active");

</script>
