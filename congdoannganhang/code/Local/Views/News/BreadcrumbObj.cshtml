@using BIZ
@using UTILS
@using Local.Helper
@model BIZ.Entity.CATEGORY_FULL

@if (Model != null)
{
    <div class="n_title"><span>@Model.Name</span></div>
    @*<div class="title9">
       <a href="@Utils.FormatUrlRewriteByType(Model.Id, Model.Name, (int)Model.Type)">@Model.Name</a>
    </div>
    <div class="clear"></div>*@
@*<div class="cate"><span class="tittle_t"><a href="@Utils.FormatUrlRewriteByType(Model.Id, Model.Name, (int)Model.Type)"></a></span></div>*@
}