@using BIZ
@using UTILS
@using WebMVC4.Helper
@model WebMVC4.Models.DocumentModel

<div class="container document-page mt20">
    <div class="row">
        <div class="col-lg-8 col-md-12 col-sm-12">
            @{Html.RenderAction("Breadcrumb", "News", new { CategoryId = Model.CategoryId });}
            <div class="mt30">
                <div class="text-center document-search">
                    <div class="form-group">
                        <label class="">Từ khóa</label>
                        <input id="txtkeyword" class="form-control" type="text" value="@ViewBag.keyword" type="text">
                    </div>
                    <div class="form-group">
                        <label class="">Từ ngày</label>
                        <input id="txtfromdate" placeholder="dd/MM/yyyy" value="@ViewBag.fromdate" class="form-control">
                    </div>
                    <div class="form-group">
                        <label class="" style="width:75px;">Đến ngày</label>
                        <input id="txttodate" placeholder="dd/MM/yyyy" value="@ViewBag.todate" class="form-control">
                    </div>
                    <div>
                        <a href="javascript:;" onclick="searchdoc();" class="btn-green">Tìm kiếm</a>
                    </div>
                </div>
                <div class="document-tabs mt30">
                    <ul>
                        @foreach (var item in Model.subcate.Select((value, i) => new { i, value }))
                        {
                            <li><a href="@Utils.FormatUrlRewriteByType(item.value.Id, item.value.Name, int.Parse(item.value.Type.ToString()), item.value.Link)">@item.value.Name </a></li>
                        }



                    </ul>
                </div>
                <div class="document-table table-responsive mt20">
                    <table>
                        <tbody>
                            <tr>
                                <th>STT </th>
                                <th>Số hiệu </th>
                                <th>Tên văn bản </th>
                                <th>Ngày ban hành </th>
                                <th>Download </th>
                            </tr>
                            @foreach (var item in Model.listdata.Select((value, i) => new { i, value }))
                            {
                                <tr>
                                    <td>@(item.i + 1) </td>
                                    <td>@item.value.Code</td>
                                    <td>
                                        <a href="@Url.Action("ViewFile","Document",new { id=item.value.Id})" target="_blank">
                                            @item.value.Name
                                        </a>
                                    </td>
                                    <td> @Convert.ToDateTime(item.value.PublishDate).ToString(@" dd'/'MM'/'yyyy") </td>
                                    <td>
                                        <div>
                                            <a href="@Url.Action("DownloadFile","Document",new { id=item.value.Id})" class="document-view">
                                                <img src="/Content/img/icon-pdf-02.svg" width="45">

                                            </a>
                                            <a href="@Url.Action("ViewFile","Document",new { id=item.value.Id})"  target="_blank" class="document-view">
                                                <span>Xem online</span>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="paging mt30">
                    <ul>
                        @{Html.RenderPartial("Pagination", new WebMVC4.Models.PaginationInfo
                            {
                                PageUrl = Url.SetParameter("Page", "!0"),
                                CurrentPage = Model.pageIndex,
                                PageSize = Model.pageSize,
                                TotalItemCount = Model.total,
                            }); }
                    </ul>
                </div>
            </div>
        </div><!--/End content left-->
        <div class="col-lg-4 col-md-12 col-sm-12 sidebar-right">
            @Html.Partial("Site_Right")
        </div><!-- end col-lg-4-->
    </div>



</div><!-- end content-->


<script type="text/javascript">


    function searchdoc() {
        var id = '@Model.CategoryId';
        var keyword = $("#txtkeyword").val();
        var fromdate = $("#txtfromdate").val();
        var todate = $("#txttodate").val();
        window.location = '/Document/Index/' + id + '/?keyword=' + keyword + '&fromdate=' + fromdate + '&todate=' + todate;
        //window.location = jQuery.query.set("keyword", keyword).set("fromdate", fromdate).set("todate", todate).set("Page", "1");
    }
</script>
