@model List<DATA.Banner>
@if (Model != null)
{



    foreach (var item in Model.Select((value, i) => new { i, value }))
    {

        <div>
            <a href="@item.value.Url" rel="nofollow" target="_blank">
                <img src="@item.value.Data" alt="@item.value.Name"  class="w-100"/>
            </a>
        </div>
    }


}