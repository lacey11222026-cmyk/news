@model DATA.Banner
@if (Model != null)
{

    <div class="best">
        <div>
            <a href="@Model.Url" rel="nofollow">
                <img width="689" src="@Model.Data" alt="@Model.Name" />
            </a>
        </div>
    </div>

}