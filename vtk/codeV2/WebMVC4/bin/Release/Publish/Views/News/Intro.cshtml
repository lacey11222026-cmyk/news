@using BIZ
@using UTILS
@using WebMVC4.Helper
@model BIZ.Entity.CATEGORY_FULL

@if (Model != null)
{
    <div class="bnr-detail">
        <img src="images/bnr-lienhe.jpg" alt="">
    </div>

    <div class="contact-page pt60 pb60 bg-gray">
        <div class="container">
            <h3 class="contact-title">@Html.Raw(Model.Description)</h3>
            @Html.Raw(Model.Contents)
        </div>

    </div>

}