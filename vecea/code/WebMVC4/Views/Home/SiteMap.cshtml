@using BIZ
@using UTILS
@using WebMVC4.Helper
@model List<BIZ.Entity.CATEGORY_FULL>
<script type="text/javascript" src="/Scripts/simpletreemenu.js"></script>

<link href="/Styles/simpletree.css" rel="stylesheet" type="text/css" />
@if (Model != null)
{
    <div align="center" style="margin-top: 20px; color: #02628A; font-weight: bold;">
        <span style="font-size: 14px">SƠ ĐỒ WEBSITE</span>
    </div>
    <ul id="treemenu2" class="treeview">
        @if (Model.Count > 0)
        {
            foreach (var item in Model.Select((value, i) => new { i, value }))
            {
            <li><a  href="@Utils.FormatUrlRewriteByType(item.value.Id, item.value.Name, int.Parse(item.value.Type.ToString()), item.value.Link)">@item.value.Name </a>
                @Html.Raw(HtmlHelpers.BindChildCategoriesSiteMap(Convert.ToInt32(item.value.Id), int.Parse(item.value.Type.ToString())))
            </li> 
            }
        }
@*<li>Item 1</li>

        <li>Folder 1
            <ul>
                <li>Sub Item 1.1</li>
                <li>Folder 1.1
                    <ul rel="open">
                        <li>Sub Item 1.1.1</li>
                        <li>Sub Item 1.1.2</li>
                        <li>Folder 1.1.1
                            <ul>
                                <li>Sub item 1.1.1.1</li>
                                <li>Sub item 1.1.1.2</li>
                                <li>Sub item 1.1.1.3</li>
                                <li>Sub item 1.1.1.4</li>
                            </ul>
                        </li>
                    </ul>
                </li>
            </ul>
        </li>

        <li>Item 2</li>*@
    </ul>

    <script type="text/javascript">

        //ddtreemenu.createTree(treeid, enablepersist, opt_persist_in_days (default is 1))
        ddtreemenu.createTree("treemenu2", false, 5)

    </script>


}