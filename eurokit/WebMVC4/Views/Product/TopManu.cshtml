@model List<BIZ.Entity.MANUFACTORY_FULL>

<div class="collection-filter-vendor">
    <button type="button" class="accordion cs-title col-sb-trigger active"> <span>Thương hiệu</span> </button>
    <div class="panel sidebar-sort" style="max-height: 272px;">
        <ul class="no-bullets filter-vendor clearfix">
            @foreach (var item in Model.Select((value, i) => new { i, value }))
            {
                <li class="menu-item">
                    @if (item.value.Id == ViewBag.ManuId)
                    {
                        <a href="@(ViewBag.CateUrl)?ManuId=@(item.value.Id)" class="active">@item.value.Title</a>
                    }
                    else
                    {
                        <a href="@(ViewBag.CateUrl)?ManuId=@(item.value.Id)">@item.value.Title</a>
                    }

                </li>
            }

        </ul>
    </div>
</div>