@model List<DATA.Banner>
<div class="bnr-list-02 bnrSmallSlider02 mt50">
    @foreach (var item in Model.Select((value, i) => new { i, value }))
    {
        <div><a href="@item.value.Url" target="_blank"><img src="@item.value.Data" /></a></div>
    }

   
</div>
   

