@using BIZ
@using UTILS
@using WebMVC4.Helper
@model WebMVC4.Models.NewsModel
@{
    var langpage = WorkContext.GetLanguage();
}
@if (Model.listdata != null)
{
    <h2 class="title-cm">VIDEO LIÊN QUAN </h2>
    <ul class="video-list row mt10">
        @foreach (var item in Model.listdata.Take(8).Select((value, i) => new { i, value }))
        {
            <li class="col-md-3 col-sm-6 col-12">
                <a href="/video.html?VideoId=@(item.value.Id)" class="news-item">
                    <img src="@item.value.MainImage?w=308&h=173">
                    <span class="mt15 news-name">@item.value.Title</span>
                </a>
            </li>
        }
    </ul>
    <div class="paging mt30">
        <ul>
            @{Html.RenderPartial("PaginationVideo", new WebMVC4.Models.PaginationInfo
                {
                    PageUrl = Url.SetParameter("Page", "!0"),
                    CurrentPage = Model.pageIndex,
                    PageSize = Model.pageSize,
                    TotalItemCount = Model.total,
                }); }
        </ul>
    </div>

   

    <script>

        function GoVideoPage(page) {
            var VideoId = '@Model.Id';
             var Cateid='@ViewBag.CategoryId'
            if (page > 0) {
                $.ajax({
                    type: "GET",
                     url: "/Video/LoadVideo/?VideoId=" + VideoId + "&page=" + page + "&CategoryId=" + Cateid,
                    success: function (data) {
                        if (data != "null") {
                            $("#lstVideo").html(data);
                        }
                    }
                });
            }

        }

    </script>
}