@using BIZ
@using UTILS
@using VAS.Helper
@model List<BIZ.Entity.CONTENT_FULL>
@if (Model != null)
{
    if (Model.Count > 0)
    {

        <div class="sidebar-newsnew mt20">
            <div class="title-cm01">
                <a href="#">News</a>
            </div>
            @foreach (var item in Model.Take(1).Select((value, i) => new { i, value }))
            {
                <div class="newsbox-cm02 mt20">
                    <a href="@item.value.LinkUrl"><img src="@item.value.MainImage?w=367&h=206&mode=crop"></a>
                    <div>
                        <a href="@item.value.LinkUrl" class="mt10">@item.value.Title</a>
                        <span class="newsdate mt10">@item.value.PublishDate.ToString("dd/MM/yyyy")</span>
                    </div>
                </div>
            }
            <div class="sidebar-newsnew-list">
                <ul class="newslist list-border mt10">
                    @foreach (var item in Model.Skip(1).Select((value, i) => new { i, value }))
                    {
                        <li>
                            <a href="@item.value.LinkUrl">@item.value.Title</a>
                            @*<span class="newsdate mt10">@item.value.PublishDate.ToString("dd/MM/yyyy")</span>*@
                        </li>
                    }
                </ul>

            </div>
        </div>




    }
}