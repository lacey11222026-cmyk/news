@using BIZ
@using UTILS
@using WebMVC4.Helper
@model BIZ.Entity.CONTENT_FULL

@{
    Layout = "~/Views/Shared/_Home.cshtml";
}

<div class="container mt20 video-page">
    <h2 class="title-cm-02"><a href="/trang-chu-video.html">Video</a></h2>

    <div class="videopage-big mt20 bg-gray">
        <div class="row align-items-center">
            <div class="col-lg-8 col-md-6 col-sm-12">
                <div class="movie">
                    <iframe src="@(Model.Url.Replace("watch?v=", "embed/"))?autoplay=1&rel=0" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen style="position: absolute; top: 0px; right: 0px; bottom: 0px; left: 0px; width: 100%; height: 100%;"></iframe>
                </div>
            </div>
            <div class="col-lg-4 col-md-6 col-sm-12">
                <div class="videopage-big-content">
                    <p class="news-name-big">@Model.Title</p>
                    <p class="mt30">@Model.IntroText</p>
                    <div class="mt30">
                        <div class="fb-like" data-href="@(Request.Url)" data-layout="button_count" data-action="like" data-show-faces="false" data-share="true"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="mt30 tags-box">
        <p class="tags-title">@Resources.Global.LastNews:</p>
        <marquee direction="left">
            
            @{Html.RenderAction("Marquee", "Home", new { lang = WorkContext.GetLanguage() });}
        </marquee>
    </div>
    <div class="mt30 video-wrap bg-gray" id="lstVideo">
        @Html.Action("LoadVideo", "Video", new { VideoId = Model.Id, page = 1, CategoryId = Model.CategoryId.GetValueOrDefault() })
       
    </div>

</div><!-- end content 01 -->


