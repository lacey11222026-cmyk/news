@using BIZ
@using UTILS
@using  WebMVC4.Helper
@model DATA.Channel

@if (Model != null)
{
    <div class="title_c1">
        @Model.Name
        @*<a href="@Utils.FormatUrlRewriteByType(Model.Id, Model.Name, (int)Model.Type)"><font class="fl">@Model.Name</font></a>*@
         
    </div>
    
    @*<div class="cate"><span class="tittle_t"><a href="@Utils.FormatUrlRewriteByType(Model.Id, Model.Name, (int)Model.Type)"></a></span></div>*@
}